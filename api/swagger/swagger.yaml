swagger: "2.0"
info:
  version: "0.0.1"
  title: We're Nerds Dude!! by The Powder Rangers
# during dev, should point to your local machine
host: localhost:10000
# basePath prefixes all resource paths 
basePath: /
# 
schemes:
  # tip: remove http to make production-grade
  - http
  - https
# format of bodies a client can send (Content-Type)
consumes:
  - application/json
# format of the responses to the client (Accepts)
produces:
  - application/json
paths:

  /routines/:
    # binds a127 app logic to a route
    x-swagger-router-controller: get_routines
    get:
      description: GET request returns array of all predefined routines
      # used as the method name of the controller
      operationId: routines
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/RoutinesResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"

  /routines/{id}:
    # binds a127 app logic to a route
    x-swagger-router-controller: get_routines_id
    get:
      description: GET request returns specific predefined routine
      # used as the method name of the controller
      operationId: routinesId
      parameters:
        - name: id
          in: path
          required: true
          type: string
          description: Enter routine id
          
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/RoutinesResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
            
  /routines/{id}/exercises:
    # binds a127 app logic to a route
    x-swagger-router-controller: get_routines_id_exercises
    get:
      description: GET request returns array of exercises for specific routine
      # used as the method name of the controller
      operationId: routinesIdExercises
      parameters:
        - name: id
          in: path
          required: true
          type: string
          description: Enter routine id
          
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/RoutinesResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
            
  /exercises:
    # binds a127 app logic to a route
    x-swagger-router-controller: get_exercises
    get:
      description: GET request returns array of all predefined exercises
      # used as the method name of the controller
      operationId: exercises
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/RoutinesResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  
  /exercises/{id}:
    # binds a127 app logic to a route
    x-swagger-router-controller: get_exercises_id
    get:
      description: GET request returns specific exercise
      # used as the method name of the controller
      operationId: exercisesId
      parameters:
        - name: id
          in: path
          required: true
          type: integer
          description: Input exercise id
          
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/RoutinesResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
    
  /users/signup:
    # binds a127 app logic to a route
    x-swagger-router-controller: users_signup

    post:
      description: POST request for user sign-up
      # used as the method name of the controller
      operationId: users
      parameters:
        - name: first_name
          in: query
          required: true
          type: string
          description: Input first name
          
        - name: last_name
          in: query
          required: true
          type: string
          description: Input last name
        
        - name: weight
          in: query
          required: true
          type: number
          description: Input user weight (lb)
          
        - name: email
          in: query
          required: true
          type: string
          description: Input email
          
        - name: password
          in: query
          required: true
          type: string
          description: Input password
          
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/RoutinesResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
            
  /users/:
    # binds a127 app logic to a route
    x-swagger-router-controller: users_login
    post:
      description: POST request for user to log-in
      # used as the method name of the controller
      operationId: usersId
      parameters:
        - name: email
          in: query
          required: true
          type: string
          description: Input email
          
        - name: password
          in: query
          required: true
          type: string
          description: Input password
          
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/RoutinesResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
            

  /users/{users_id}/routines:
    # binds a127 app logic to a route
    x-swagger-router-controller: users_id_routines
    
    get:
      description: GET request for user's custom routine
      # used as the method name of the controller
      operationId: get_user_id_routines
      parameters:
        - name: users_id
          in: path
          required: true
          type: integer
          description: Input users id
        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/RoutinesResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
    
    post:
      description: POST request for user to create custom routine
      # used as the method name of the controller
      operationId: post_users_id_routine
      parameters:
        - name: users_id
          in: path
          required: true
          type: integer
        - name: name
          in: query
          required: true
          type: string
        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/RoutinesResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"

    patch:
      description: PATCH request for user to update custom routine
      # used as the method name of the controller
      operationId: patch_users_id_routine
      parameters:
        - name: users_id
          in: path
          required: true
          type: integer
        - name: name
          in: query
          required: true
          type: string
        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/RoutinesResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"

    delete:
      description: DELETE request for user's custom routine
      # used as the method name of the controller
      operationId: delete_users_id_routine
      parameters:
        - name: users_id
          in: path
          required: true
          type: integer
        - name: name
          in: query
          required: true
          type: string
        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/RoutinesResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"

  /users/{users_id}/favorites:
    # binds a127 app logic to a route
    x-swagger-router-controller: users_id_favorites
    
    get:
      description: GET request for user's custom routine
      # used as the method name of the controller
      operationId: get_users_id_favorites
      parameters:
        - name: users_id
          in: path
          required: true
          type: integer
        - name: name
          in: query
          required: true
          type: string
        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/RoutinesResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
    
    post:
      description: POST request for user to create custom routine
      # used as the method name of the controller
      operationId: post_users_id_favorites
      parameters:
        - name: users_id
          in: path
          required: true
          type: integer
        - name: name
          in: query
          required: true
          type: string
        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/RoutinesResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"

  /users/{users_id}/favorites/{id}:
    # binds a127 app logic to a route
    x-swagger-router-controller: users_id_favorites_id
    
    get:
      description: GET request for user's custom routine
      # used as the method name of the controller
      operationId: get_users_id_favorites_id
      parameters:
        - name: users_id
          in: path
          required: true
          type: integer
        - name: id
          in: path
          required: true
          type: integer
        - name: name
          in: query
          required: true
          type: string
        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/RoutinesResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
    
    delete:
      description: DELETE request for user to remove routine from favorites
      # used as the method name of the controller
      operationId: delete_users_id_favorites_id
      parameters:
        - name: users_id
          in: path
          required: true
          type: integer
        - name: id
          in: path
          required: true
          type: integer
        - name: name
          in: query
          required: true
          type: string
        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/RoutinesResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"

  /users/{users_id}/log:
    # binds a127 app logic to a route
    x-swagger-router-controller: users_id_log
    
    get:
      description: GET request for array of all log entries
      # used as the method name of the controller
      operationId: get_users_id_log
      parameters:
        - name: users_id
          in: path
          required: true
          type: integer
        - name: name
          in: query
          required: true
          type: string
        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/RoutinesResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
    
    post:
      description: POST request for user to add routine to log
      # used as the method name of the controller
      operationId: post_users_id_log
      parameters:
        - name: users_id
          in: path
          required: true
          type: integer
        - name: routines_id
          in: query
          required: true
          type: integer
        - name: date_time
          in: query
          required: true
          type: string
        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/RoutinesResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"

  /users/{users_id}/log/{id}:
    # binds a127 app logic to a route
    x-swagger-router-controller: users_id_log_id
    
    get:
      description: GET request for user's custom routine
      # used as the method name of the controller
      operationId: get_users_id_log_id
      parameters:
        - name: users_id
          in: path
          required: true
          type: integer
        - name: id
          in: path
          required: true
          type: integer
        - name: name
          in: query
          required: true
          type: string
        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/RoutinesResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  
    patch:
      description: PATCH request for user to update log entry
      # used as the method name of the controller
      operationId: patch_users_id_log_id
      parameters:
        - name: users_id
          in: path
          required: true
          type: integer
        - name: id
          in: path
          required: true
          type: integer
        - name: name
          in: query
          required: true
          type: string
        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/RoutinesResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  
    delete:
      description: DELETE request for user to remove routine from favorites
      # used as the method name of the controller
      operationId: delete_users_id_log_id
      parameters:
        - name: users_id
          in: path
          required: true
          type: integer
        - name: id
          in: path
          required: true
          type: integer
        - name: name
          in: query
          required: true
          type: string
        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/RoutinesResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"

  /users/{users_id}/weight:
    # binds a127 app logic to a route
    x-swagger-router-controller: users_id_weight
    
    get:
      description: GET request for array of all weight log entries
      # used as the method name of the controller
      operationId: get_users_id_weight
      parameters:
        - name: users_id
          in: path
          required: true
          type: integer
        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/RoutinesResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
    
    post:
      description: POST request for user to weight log entry
      # used as the method name of the controller
      operationId: post_users_id_weight
      parameters:
        - name: users_id
          in: path
          required: true
          type: integer
        - name: weight
          in: query
          required: true
          type: number
        - name: date_time
          in: query
          required: true
          type: string
        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/RoutinesResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"

  /users/{users_id}/weight/{id}:
    # binds a127 app logic to a route
    x-swagger-router-controller: users_id_weight_id
    
    get:
      description: GET request for user's specific weight log entry
      # used as the method name of the controller
      operationId: get_users_id_weight_id
      parameters:
        - name: users_id
          in: path
          required: true
          type: integer
        - name: id
          in: path
          required: true
          type: integer

        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/RoutinesResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  
    patch:
      description: PATCH request for user to update weight log entry
      # used as the method name of the controller
      operationId: patch_users_id_weight_id
      parameters:
        - name: users_id
          in: path
          required: true
          type: integer
        - name: id
          in: path
          required: true
          type: integer
        - name: weight
          in: query
          required: false
          type: string
        - name: date_time
          in: query
          required: false
          type: string
        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/RoutinesResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  
    delete:
      description: DELETE request for user to remove entry from weight log
      # used as the method name of the controller
      operationId: delete_users_id_weight_id
      parameters:
        - name: users_id
          in: path
          required: true
          type: integer
        - name: id
          in: path
          required: true
          type: integer
        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/RoutinesResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"



  /swagger:
    x-swagger-pipe: swagger_raw
# complex objects have schema definitions
definitions:
  RoutinesResponse:
    required:
      - message
    properties:
      message:
        type: string
  ErrorResponse:
    required:
      - message
    properties:
      message:
        type: string
        

